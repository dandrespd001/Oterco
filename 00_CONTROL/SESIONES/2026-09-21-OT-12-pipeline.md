# Acta 2026-09-21 — OT-12 Pipeline y candidato independiente

**Sesión coordinador:** ses_f452eb8eeffeQJGEN8CFk4jaxV · **Implementador:** ot-implementador (ses_f3bf42bffffeAnfrVzQMDSjMbP + ajustes ses_f3be659e0ffeq7IzjYUIsega2w) · **Revisor:** ot-revisor (ses_f3beb8fb9ffeOScTEdzzgcg1b2).

## Resultados

- Scripts `apps/oterco/scripts/release/`: `candidato.mjs` (manifiesto con siteId OTERCO-<slug>, propósito candidato-tecnico|candidato-comercial-aprobado, commit/sha, versiones, dominio/contacto pendienteAP, hashes por fichero y árbolHash, presupuestos re-ejecutados; copia de dist sin recompilar), `verificar.mjs` (12 checks, re-hash, detector PA, cuotas Free), `promocionar.mjs` (promoción local con copia idéntica + humo efímero; sin recompilación).
- Negativos reproducidos: alteración de byte, siteId PA-*, comercial sin aprobaciones, marca PA por contenido/nombre, manifiesto manipulado, dist ausente → todos rechazo exit≠0. Suite: 231 tests · 226 pass · 5 skip.
- `releases/` ignorado (gitignore anclado); artefactos nunca en commit; sin YAML, sin secretos; scripts remotos NO implementados.

## Ajustes post-revisión

H-1 (origen.rel, sin rutas absolutas del operador), H-3 (limpieza con --forzar), H-4 (resumen de integridad incluso con fallos previos), H-2 (extracto completado), H-7/H-8 (redacción y anclaje gitignore). H-5/H-6: queda como límite documentado (manifiesto no firmado: fuera del modelo local; registrar en el manual de OT-13).

## Casos

- Q044 ✓ · Q045 ✓ (7 rechazos) · Q049 ✓ (persistido en acta y registros) · Q053 ✓ · **Q046 documental** (sin CI remota; materializar workflow y auditar = operador OT-16/17).

## Próximo paso

OT-13 — Recuperación y manual de edición (runbook de edición y备份 recuperación local; sin prometer recuperación de DNS/buzón).
